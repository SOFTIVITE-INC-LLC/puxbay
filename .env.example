# Django Settings
SECRET_KEY=oy8m7pm@5h!l6_e3rl#bl-&$$8f^x^n#s_brp6_99-z&+@c+01
DEBUG=True

# Fernet Encryption Key (generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())")
FERNET_KEY=eKgCfI7yND_KPMjoCBycwO0UyDSYFfzw3omFVuxzEGQ=

# Allowed Hosts (comma-separated)
ALLOWED_HOSTS=localhost,127.0.0.1,.localhost

# Database Configuration
DB_NAME=puxbay
DB_USER=puxbay
DB_PASSWORD=Thinkce@softivitepuxbay
DB_HOST=localhost
DB_PORT=5432

# Database Replicas (5 read-only standbys for load distribution)
# Set all to same as primary if not using replication
DB_REPLICA_HOST=localhost
DB_REPLICA1_PORT=5433
DB_REPLICA2_PORT=5434
DB_REPLICA3_PORT=5435
DB_REPLICA4_PORT=5436
DB_REPLICA5_PORT=5437
REDIS_URL=redis://127.0.0.1:6379/1

# Domain
DOMAIN_URL=http://localhost:8000/

# Stripe Configuration
STRIPE_PUBLISHABLE_KEY=pk_test_your_key_here
STRIPE_SECRET_KEY=sk_test_your_key_here

# Paystack Configuration
PAYSTACK_PUBLIC_KEY=pk_test_your_key_here
PAYSTACK_SECRET_KEY=sk_test_your_key_here

# CORS (comma-separated origins)
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8000

# =============================================================================
# EMAIL CONFIGURATION
# =============================================================================
# For development, leave EMAIL_BACKEND commented out (uses console backend)
# For production, uncomment and configure one of the providers below

# Email Backend
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend

# --- Gmail Configuration ---
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_HOST_USER=your-email@gmail.com
# EMAIL_HOST_PASSWORD=your-app-password
# EMAIL_USE_TLS=True
# EMAIL_USE_SSL=False

# --- SendGrid Configuration ---
# EMAIL_HOST=smtp.sendgrid.net
# EMAIL_PORT=587
# EMAIL_HOST_USER=apikey
# EMAIL_HOST_PASSWORD=your-sendgrid-api-key
# EMAIL_USE_TLS=True

# --- Mailgun Configuration ---
# EMAIL_HOST=smtp.mailgun.org
# EMAIL_PORT=587
# EMAIL_HOST_USER=postmaster@your-domain.mailgun.org
# EMAIL_HOST_PASSWORD=your-mailgun-password
# EMAIL_USE_TLS=True

# --- Amazon SES Configuration ---
# EMAIL_HOST=email-smtp.us-east-1.amazonaws.com
# EMAIL_PORT=587
# EMAIL_HOST_USER=your-ses-smtp-user
# EMAIL_HOST_PASSWORD=your-ses-smtp-password
# EMAIL_USE_TLS=True

# Sender Information
DEFAULT_FROM_EMAIL=noreply@yourdomain.com
EMAIL_SUBJECT_PREFIX=[PUXBAY TEAM] 

# =============================================================================
# API SECURITY CONFIGURATION
# =============================================================================

# Enable API request audit logging (logs all API requests to database)
ENABLE_API_AUDIT_LOGGING=True

# IP Whitelist for sensitive endpoints (comma-separated IPs or CIDR ranges)
# Leave empty to allow all IPs
# Example: WHITELISTED_IPS=192.168.1.1,10.0.0.0/24,172.16.0.0/16
WHITELISTED_IPS=

# Webhook signature secret (generate a random string for production)
# Used for HMAC-SHA256 signature validation
WEBHOOK_SECRET=your-webhook-secret-change-in-production

# Enable rate limiting
RATELIMIT_ENABLE=True

# =============================================================================
# EMAIL SERVER CONFIGURATION
# =============================================================================

# Email Backend (smtp, console, or file)
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend

# SMTP Server Settings
EMAIL_HOST=mail.puxbay.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_USE_SSL=False

# Authentication
EMAIL_HOST_USER=noreply@puxbay.com
EMAIL_HOST_PASSWORD=Thinkce@softivite123

# From Addresses
DEFAULT_FROM_EMAIL=POS System <noreply@puxbay.com>
SERVER_EMAIL=server@puxbay.com

# Timeout (seconds)
EMAIL_TIMEOUT=30
